<nav class="navbar navbar-expand-lg navbar-light bg-light" id="nav">
    <div class="w-25">
        <img src='build/popcorn.png' class="img-fluid w-25" alt="" />
    </div>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
            <a class="nav-link active" href="{{ path('app_index') }}">Home <span class="sr-only">(current)</span></a>
            <a class="nav-link active" href="{{ path('program_index') }}">Programs <span class="sr-only">(current)</span></a>
            <a class="nav-link active" href="{{ path('category_index') }}">Category <span class="sr-only">(current)</span></a>
        </div>
    </div>
</nav>
